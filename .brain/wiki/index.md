# Index

Generated by `.brain/scripts/brain-index.sh` — do not edit by hand.

277 source pages of 679 tracked files · 49 directories · 42 concepts · 32 entities · 3 synthesis

## Synthesis

- [[Documented Stack Vs Actual Stack]] — The agent instructions loaded into every session describe a stack this repo does
- [[Frontend Scaffold Reality]] — The frontend looks larger than it is. This page states exactly how much of i
- [[Repo Hazards]] — Traps in this repository that will silently waste your time. Read this before your

## Concepts

- [[Accessibility Compliance]] — What it is. IS 5568 (WCAG 2.0 AA) is a legal requirement for a public Israeli site, not a
- [[Advisory Lock]] — What it is. SELECT pg_advisory_xact_lock(hashtext(<key>)) executed as the first statemen
- [[Append Only Terms Versions]] — What it is. terms_versions holds one immutable row per publication of a boutique's
- [[Audit Trail]] — What it is. The audit_log table — one row per staff action inside a boutique, tenant-scoped
- [[CSRF Origin Check]] — What it is. Defence in depth for the cookie-authenticated /manage surface: a mutating
- [[Database Migrations]] — Every schema change is a versioned, ordered Alembic revision. The chain is linear — each revisio
- [[DB Test Marker]] — The backend suite is split by pytest marker so the fast tests stay fast.
- [[Design Tokens]] — What it is. One @theme block in frontend/packages/ui/src/theme.css is the single source of
- [[Enumeration Resistance]] — What it is. The house rule that a failure body describes what happened to the caller, never
- [[Fail Closed Defaults]] — When something is missing or malformed, deny rather than allow.
- [[Fail Fast Configuration]] — Invalid or missing configuration aborts startup rather than surfacing as a confusing runtime err
- [[Full Replace Update Semantics]] — What it is. The house rule for every entity update endpoint: the request model declares every
- [[Graceful Degradation]] — What it is. A deployment with no media bucket or no SMS provider is a supported
- [[Hebrew First UX]] — What it is. Hebrew is not a localization of an English product — it is the only locale a
- [[Input Validation At The Boundary]] — What it is. Four layers, each answering a different question, with one number written once and
- [[Jerusalem Time]] — What it is. Storage and the wire are UTC. The boutique's calendar is Asia/Jerusalem, and
- [[Least Privilege Database Role]] — The application connects to PostgreSQL as a non-owner role so that FORCE ROW LEVEL SECURITY actu
- [[Media Storage]] — What it is. The port that dress photos go through — a Protocol in
- [[Media Upload Pipeline]] — What it is. The three-call sequence by which a dress photo becomes visible: presign →
- [[One Time Passcode]] — What it is. The customer identity realm. A bride never gets a password — she proves possession
- [[Opaque Token Hashing]] — What it is. Every bearer credential this product mints — the staff session cookie and the
- [[Owner Authentication]] — What it is. The staff auth realm: email + password login against staff_users, exchanged for
- [[Package Layout]] — What it is. The shape every feature package under backend/app/ takes. Knowing it means you
- [[Partial Unique Index]] — What it is. CREATE UNIQUE INDEX … WHERE <predicate> — this repo's concurrency primitive and
- [[Planning Artifacts Vs Implementation]] — What it is. .planning/ is this project's decision record — 21 specs, 17 plans, ten epics,
- [[Platform Audit Log]] — What it is. The platform_audit_log table — a cross-tenant record of what a platform
- [[Ports And Adapters]] — What it is. The two external dependencies this backend has — object storage and SMS — si
- [[Product Policy Vs Deployment Identity]] — What it is. A standing rule about where a number is allowed to live.
- [[Public Wire Schemas]] — What it is. The response models served to anonymous visitors, kept in their own module
- [[Rate Limiting]] — What it is. FixedWindowRateLimiter in backend/app/auth/rate_limit.py — an in-process
- [[Repository Pattern]] — What it is. One stateless class per table under
- [[Role Based Access Control]] — What it is. Every route under /manage carries a RoleGate, and the set of roles is
- [[Row Level Security]] — The mechanism that makes this product multi-tenant. Isolation is enforced by
- [[RTL And Bidi Isolation]] — What it is. Both apps ship <html lang="he" dir="rtl">
- [[Soft Delete]] — What it is. Every table in this schema carries deleted_at TIMESTAMPTZ and nothing is ever
- [[Stored XSS Prevention]] — What it is. Two things stored by one party render for another: the boutique's own settings and
- [[Tenant Context]] — The per-connection binding that tells PostgreSQL which boutique the current request belongs to.
- [[Tenant Isolation]] — The guarantee that one boutique can never observe or modify another's data.
- [[Tenant Provisioning]] — What it is. The operator-only lifecycle for a boutique: create the tenants row, its firs
- [[Tenant Resolution]] — Turning an inbound HTTP request into a known tenant, before any database work happens.
- [[Test Doubles]] — What it is. The house testing style: hand-written Fake classes that structurally match the
- [[Vendored Toolkit Applicability Audit]] — What it is. The method for deciding whether a file under .claude/ applies to this repo,

## Entities

- [[Alembic]] — Schema migration tool for SQLAlchemy. Every schema change is a versioned revision.
- [[AnyIO]] — The async runtime Starlette and FastAPI are built on. This repo touches i
- [[Argon2]] — The staff password hasher. argon2-cffi>=25.1.0, declared in
- [[axe-core]] — The automated accessibility checker. axe-core 4.12.1, entered two different ways: AxeBuilder fro
- [[Boto3]] — The AWS SDK, used for S3 and nothing else. boto3>=1.36 in
- [[Docker]] — Container runtime. Required locally only for Testcontainers to run the -m db suite; the applicat
- [[FastAPI]] — The ASGI web framework this backend is built on. Routers, dependency injection, and Pydantic-bac
- [[Fontsource]] — Self-hosted webfonts. @fontsource/frank-ruhl-libre 5.3.0 (display) and @fontsource/assistant 5.3
- [[i18next]] — All user-facing copy in both apps. i18next 26.3.6 with react-i18next 17.0.11, initialised identi
- [[Intl API]] — The platform's built-in Intl.DateTimeFormat / Intl.NumberFormat is the only date, time and money
- [[jsdom]] — The DOM implementation every Vitest suite runs in. Version ^29.1.1, set as
- [[MinIO]] — The S3-compatible server that stands in for AWS in the test suite. Pinned as
- [[openapi-typescript]] — Generates TypeScript types from the backend's OpenAPI schema. Version ^7.5.0, a
- [[oxlint]] — The only JavaScript/TypeScript linter here — there is no ESLint, no Prettier and no
- [[Playwright]] — The only real-browser test layer in the repo. @playwright/test 1.62 plus
- [[pnpm]] — Package manager and workspace runner for the whole frontend. Version pinned as "packageManager":
- [[PostgreSQL]] — The database. Chosen specifically for row-level security, which is the mechanism enforcing tenan
- [[Pydantic Settings]] — Environment-driven, type-validated application configuration.
- [[Pydantic]] — Request/response schema validation, used by FastAPI for automatic parsing and error responses.
- [[pytest]] — The test runner for the backend suite in backend/tests/.
- [[React]] — The view layer for both frontend apps. React 19.2.7, rendered as a plain client-side SPA by Vite
- [[Spartan Toolkit]] — A vendored agent-tooling toolkit installed under .claude/ — 71 slash commands, 22 rule files, 9 
- [[SQLAlchemy]] — The ORM and Core layer for all database access. Version 2.x, async style.
- [[Starlette]] — The ASGI toolkit FastAPI is built on. Not a declared dependency in
- [[Tailwind CSS]] — The styling system for both apps and the shared component package. Tailwind 4.3.3, CSS-first: th
- [[Testcontainers]] — Spins up a real PostgreSQL instance in Docker for the -m db test suite, so row-level security is
- [[Testing Library]] — How every component test in this repo queries the DOM. @testing-library/reac
- [[TypeScript]] — The language for the whole frontend. Declared ^5.7.0 in every package, resolved to 5.9.3 in fron
- [[uv]] — The Python package and environment manager for the backend. Replaces pip/virtualenv/poetry.
- [[Uvicorn]] — The ASGI server that runs the FastAPI app in development.
- [[Vite]] — Dev server and production bundler for both apps. Vite 8.1.5 (rolldown-based), with @vitejs/plugi
- [[Vitest]] — The frontend unit and component test runner. Vitest 4.1.10 with environment: "jsdom" (jsdom 29),

## Sources

Directory pages only. Leaf files are listed inside each directory page.

- [[backend/_index]] — The Python 3.13 / FastAPI service: the whole API, the async SQLAlchemy data layer, the raw-SQL m (191 files)
  - [[backend/app/_index]] — The application package. One sub-package per bounded surface (auth, booking, catalog, boutique,  (113 files)
    - [[backend/app/api/_index]] — A scaffold vestige. It holds only the /health probe — every other router lives in its own domain (3 files)
      - [[backend/app/api/routes/_index]] — The health probe, and nothing else. (2 files)
    - [[backend/app/auth/_index]] — Owner and staff identity: password verification, opaque session tokens, the session cookie, and  (11 files)
    - [[backend/app/booking/_index]] — The booking engine and the most intricate code in the repo: the concurrency-safe seat claim, the (14 files)
    - [[backend/app/boutique/_index]] — Owner settings: profile, opening hours, appointment types, and the append-only terms versions a  (5 files)
    - [[backend/app/catalog/_index]] — The owner's dress catalogue — dresses, size variants, and the presign/confirm media pipeline tha (6 files)
    - [[backend/app/core/_index]] — The settings object, and only that. Deployment identity lives here; product policy deliberately  (2 files)
    - [[backend/app/db/_index]] — Engine, session factory, the tenant-binding wrapper that sets the RLS context, and the repositor (20 files)
      - [[backend/app/db/repositories/_index]] — Every SQL statement in the product. One class per table, each tenant-scoped by RLS with an expli (16 files)
    - [[backend/app/models/_index]] — The SQLAlchemy declarative models — one per table, each mirroring a raw-SQL migration, plus the  (19 files)
    - [[backend/app/notifications/_index]] — The SMS port and the OTP primitive: a provider-agnostic sender, its fake and unconfigured adapte (8 files)
    - [[backend/app/platform/_index]] — The operator-side surface — tenant provisioning and the INSERT-only platform audit log. CLI-only (3 files)
    - [[backend/app/storage/_index]] — The media abstraction: an S3 adapter, an in-memory one for tests, and an unconfigured one that a (5 files)
    - [[backend/app/storefront/_index]] — The public, anonymous read surface: the catalogue a bride browses and the slot grid she books fr (5 files)
    - [[backend/app/tenancy/_index]] — Subdomain-to-tenant resolution and the middleware that runs it — the thing every RLS session con (4 files)
  - [[backend/tests/_index]] — The test suite. Unmarked tests run everywhere; db-marked tests need Docker (Testcontainers Postg (60 files)
- [[frontend/_index]] — The pnpm workspace: two Vite SPAs and the packages they share. No Next.js, despite what .claude/ (169 files)
  - [[frontend/apps/_index]] — The two shipped applications — the public storefront and the authenticated owner console. (99 files)
    - [[frontend/apps/manage/_index]] — The owner and shift-manager console. Cookie-authenticated against /manage, Hebrew-only, and deli (47 files)
      - [[frontend/apps/manage/public/_index]] — Static assets served as-is. (3 files)
        - [[frontend/apps/manage/src/__tests__/_index]] — Vitest suites for the console, including the axe passes that carry F15's accessibility coverage  (13 files)
      - [[frontend/apps/manage/src/_index]] — The console source. App.tsx is the whole navigation model — a single useState over section keys, (39 files)
        - [[frontend/apps/manage/src/assets/_index]] — Brand artwork. (1 files)
        - [[frontend/apps/manage/src/components/_index]] — One component per console section, plus the booking sub-views. Four of the older sections hardco (13 files)
        - [[frontend/apps/manage/src/i18n/_index]] — Hebrew strings, and the Arabic bundle that ships untranslated with the Hebrew standing in as pla (3 files)
        - [[frontend/apps/manage/src/lib/_index]] — Pure helpers: the Jerusalem-zoned formatters and the shared booking view helpers that would othe (2 files)
        - [[frontend/apps/manage/src/test/_index]] — Vitest setup, including the jsdom <dialog> stub the confirm modals need. (1 files)
    - [[frontend/apps/storefront/_index]] — The public per-tenant boutique site: catalogue, dress pages, the booking flow, and the tokenized (52 files)
      - [[frontend/apps/storefront/public/_index]] — Static assets, including the robots.txt that keeps unlaunched boutiques out of search results. (4 files)
        - [[frontend/apps/storefront/src/__tests__/_index]] — Vitest suites for the storefront, and the largest test surface in the repo. (16 files)
      - [[frontend/apps/storefront/src/_index]] — The storefront source, including a hand-rolled router — there is no routing library. (43 files)
        - [[frontend/apps/storefront/src/components/_index]] — Storefront-only presentational components — the ones too site-specific to earn a place in packag (9 files)
          - [[frontend/apps/storefront/src/components/booking/_index]] — The booking flow's pickers, minus the slot picker, which was promoted into packages/ui when the  (2 files)
        - [[frontend/apps/storefront/src/i18n/_index]] — Hebrew strings plus the untranslated Arabic bundle. (3 files)
        - [[frontend/apps/storefront/src/lib/_index]] — Pure helpers for contact links and opening-hours prose. (2 files)
        - [[frontend/apps/storefront/src/routes/_index]] — One component per route, wired by the hand-rolled router. (6 files)
        - [[frontend/apps/storefront/src/test/_index]] — Vitest setup. (1 files)
  - [[frontend/e2e/_index]] — Playwright: journey specs and the axe accessibility specs, run against vite preview of both buil (5 files)
  - [[frontend/packages/_index]] — The shared workspace packages. (59 files)
    - [[frontend/packages/api-client/_index]] — A deliberate non-implementation. Codegen was considered and declined; the header records why, an (3 files)
      - [[frontend/packages/api-client/src/_index]] — An empty export. See the package page for the decision it encodes. (1 files)
    - [[frontend/packages/ui/_index]] — The shared component library. It has no i18next dependency by design — every string arrives as a (56 files)
        - [[frontend/packages/ui/src/__tests__/_index]] — Vitest suites pinning each primitive's contract, including the design-token guard that certifies (17 files)
      - [[frontend/packages/ui/src/_index]] — The package source and its public export surface. (53 files)
        - [[frontend/packages/ui/src/components/_index]] — Every shared primitive and composite, from Button to the promoted SlotPicker. (29 files)
        - [[frontend/packages/ui/src/lib/_index]] — Pure helpers: the class joiner, the Jerusalem constant and opening-hours maths, and URL helpers. (3 files)
        - [[frontend/packages/ui/src/test/_index]] — Vitest setup shared by the package's suites. (1 files)
