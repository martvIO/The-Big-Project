---
tags: [frontend, typescript, test]
sources: [frontend/apps/storefront/src/__tests__]
created: 2026-07-31
updated: 2026-07-31
# --- .brain extensions (see .brain/CLAUDE.md § Deviations) ---
path: frontend/apps/storefront/src/__tests__
blob: d1eee583e0567fb81a8896cc7c0a0f36148b4c7d
commit: 13d107b8b16f1f2676f1cdb1c994f95822fe2ba0
kind: directory
applicability: active
---

# frontend/apps/storefront/src/__tests__/

**Purpose.** Vitest suites for the storefront, and the largest test surface in the repo.

**Parent.** [[frontend/apps/storefront/src/_index]]

## Files

- [[frontend/apps/storefront/src/__tests__/AboutPage.test.tsx]] — The trust-surface suite: load/failure states, the sole-`<h1>` invariant in every one of them, the opening-hours week (grouping, the day the wire omits, the lunch break), the exceptions list, the address-without-a-map case, and the fact…
- [[frontend/apps/storefront/src/__tests__/BookPage.test.tsx]] — The largest suite in the repo (~2160 lines) and the spine of the storefront booking feature: the five-step flow walked end to end (slot → details → terms → verify → confirm), the step guards, the OTP sub-states including a cooldown timer…
- [[frontend/apps/storefront/src/__tests__/CatalogPage.test.tsx]] — The landing page's suite: the four-way composition of two independent fetches (identity and collection) across loading/failure, load-more paging, the card states (priced, hidden price, reserved, photo-less), the one-shot refetch on a stale…
- [[frontend/apps/storefront/src/__tests__/ContactCard.test.tsx]] — Four tests, all about one decision: when `ContactCard` renders *nothing at all* versus when it renders the paper card. The wrapper's only job beyond `ContactPanel` is that null-return, so this file is the whole contract.
- [[frontend/apps/storefront/src/__tests__/DescriptionClamp.test.tsx]] — Three tests for the one thing about the clamp that no other suite can reach: the toggle appearing and disappearing *because the A11yMenu's text-size boost changed the layout*, with no resize event to hang it on.
- [[frontend/apps/storefront/src/__tests__/DressPage.test.tsx]] — The dress-detail suite: the always-present way back, the gallery's position-based image names, the facts column (price, sizes in words, the reserved-but-still-bookable CTA), the description clamp's ARIA wiring, the archived-dress branch…
- [[frontend/apps/storefront/src/__tests__/ManageBookingPage.test.tsx]] — F16's `/b/{token}` suite, organised around the page's six states — S (loading), L (upcoming), L2 (attendance confirmed), C (cancelled), P (past), X (invalid link) plus R (retryable failure) — with the cancel two-step, three focus…
- [[frontend/apps/storefront/src/__tests__/ShareButton.test.tsx]] — Six tests over the share button's three platform branches — native sheet, clipboard fallback, neither — with the file's real weight going to the two share *rejections* that must not be treated alike: a dismissed sheet (nothing failed) and…
- [[frontend/apps/storefront/src/__tests__/StorefrontLayout.test.tsx]] — The contract test for the shell every storefront route renders inside: that the skip link's fragment resolves to the *same* `<main>` the router focuses, that the statutory footer links appear on every route, that the whole app issues…
- [[frontend/apps/storefront/src/__tests__/accessibility.test.tsx]] — The audit suite for הצהרת נגישות — the accessibility statement page. It guards what an IS 5568 §35 auditor actually checks: real document semantics, the standard named precisely, one entry per tool the A11yMenu ships, the limitations…
- [[frontend/apps/storefront/src/__tests__/api.test.ts]] — The full contract test for the storefront's hand-written fetch client: error extraction from the house envelope, the three fallback paths that must produce an `ApiError` rather than a raw `SyntaxError`, `credentials: "omit"` on **every**…
- [[frontend/apps/storefront/src/__tests__/contact.test.ts]] — The unit suite for the two link builders in `lib/contact` — `waPhone` (an Israeli mobile to a `wa.me` digit string) and `wazeUrl` — written specifically to exercise the branches no fixture in the repo would otherwise reach.
- [[frontend/apps/storefront/src/__tests__/hoursText.test.ts]] — The unit suite for the pure opening-hours text layer — date/time formatting, grouping the wire's per-window rows into per-day rules, folding exceptions into a lookup, and `todayLine()`, the single highest-stakes string on an…
- [[frontend/apps/storefront/src/__tests__/i18n-keys.test.ts]] — A source scanner, not a rendering test: it reads every dotted literal key out of the app's own `.ts`/`.tsx` files with `node:fs`, resolves each one against the `he.ts` bundle **without going through `t()`**, and asserts it lands on real…
- [[frontend/apps/storefront/src/__tests__/router.test.tsx]] — The contract test for the hand-rolled router: `matchRoute`'s full path table (including the closed booking-step set and the `/b/{token}` manage link), `Link` and the document-level click delegation deciding what to intercept, and the…
- [[frontend/apps/storefront/src/__tests__/validation.test.ts]] — The mirror test: it pins the client-side booking validators to the *backend's* rules character for character — the two length bounds, the two different control-character sets, the order the three checks run in, and the exact accept/reject…
