# Plan: Owner Settings, Toggles & Structured Cancellation Policy

**Branch**: `feature/owner-settings` off `main` · **Spec**: `../specs/owner-settings.md` (rev 2) · **Epic**: E2 Feature 7

Sequential TDD. No Docker locally: unit + fast-API tests run local red→green; repository/service db tests and the RLS scan are CI-gated. Money is integer agorot everywhere. At build start: E2 epic table F7 → `building` + spec/plan links (E1 table convention).

### Task 1 — Schema + models + role-guard extension
`migrations/versions/0005_boutique_settings.py` (revision `"0005"`, down `"0004"`): `appointment_types`, `availability_rules` (incl. `capacity`), `availability_exceptions`, `terms_versions` — `_STANDARD` columns (**already include `tenant_id`** — do not re-add), CHECK constraints per spec, updated-at triggers (NOT on `terms_versions`), **partial** unique indexes for `(tenant_id,name)` / `(tenant_id,date)` and a **plain** unique for `(tenant_id,version)`, per-table `GRANT … TO app_user` + `enable_tenant_rls(table)`; `terms_versions`: REVOKE ALL → GRANT SELECT, INSERT only. Models in `app/models/` + `AppointmentAudience` StrEnum. **Extend `verify_database_role`**: also fail when `current_user` owns tables in `public` (test: guard trips under the owner URL; consumed by F2 B6). Covered by `migrated_db` + metadata scan + guard tests.

### Task 2 — Repositories + service (db-tested)
`app/db/repositories/{appointment_types,availability,terms}.py` — house style (methods take `session` + explicit `tenant_id` predicate + `deleted_at IS NULL`, keyword-only inserts). **`TenantsRepository.merge_settings(tenant_id, profile, toggles)`** — single atomic `UPDATE tenants SET settings = settings || :patch::jsonb WHERE id = :tenant_id AND deleted_at IS NULL` (platform-scoped repo pattern, no tenant_session — tenants has no RLS). `app/boutique/service.py` `BoutiqueSettingsService(session_factory)`: settings merge, appointment-type CRUD (IntegrityError → duplicate-name result), weekly-rules replace under `pg_advisory_xact_lock(tenant_id)` (validate → soft-delete → insert, one tx), exceptions add/remove (one-sided-times validation, duplicate-date mapping), terms `create_next_version` (max+1; IntegrityError → **fresh `tenant_session` retry**, then conflict result) + terms-creation throttle via `FixedWindowRateLimiter`. Tests first (`tests/test_boutique_service.py`, db): CRUD, advisory-lock concurrency test, version race with fresh-session retry, terms UPDATE/DELETE denied at DB, CHECK rejects, **concurrent sibling-key settings merge preservation**, cross-tenant invisibility per resource; plus local pure-validation units (bounds, overlaps, maps_url scheme allowlist, phone charset, lengths).

### Task 3 — API routes (fast tests local, integration in CI)
`app/boutique/{schemas,router}.py`: routes per spec; `get_current_staff` + `get_current_tenant`; **Origin-vs-host CSRF middleware for mutating `/manage` routes**; scoped `RequestValidationError` handler emitting house-shape 400 (note: also normalizes auth-route malformed bodies — adjust existing tests if they assert 422); domain error handlers (400/409 codes per spec); terms history paginated (last 50 + offset). Router + handlers registered in `create_app()`. `tests/test_boutique_api.py` first (fake service + hardcoded `TenantContext`): wiring, 401s, validation 400s, 409 mappings, CSRF rejection, error-shape conformance. One db-gated end-to-end: owner logs in (real `AuthService`) → configures everything → reads it back.

### Task 4 — Manage UI + dev proxy + frontend test bootstrap
`apps/manage`: **verify Vitest↔Vite-8 compatibility first** (pin verified major or standalone `vitest.config`); add Vitest + Testing Library + jsdom (devDeps + `test` script; `pnpm-lock.yaml` update — CI is `--frozen-lockfile`). CI frontend job gains `pnpm -r --if-present test`; `Makefile` gains a matching `fe-test` target. **`vite.config.ts` `server.proxy`** for `/manage` + `/health` → `http://localhost:8000` with Host preserved (`changeOrigin: false`); README note: develop at `http://{slug}.localtest.me:5173`; never add CORS. `src/api.ts` typed fetch helper (`credentials: "include"`, snake_case types). Login gate on `/manage/auth`; four sections (Profile & toggles / Hours & exceptions / Appointment types / Cancellation policy) with the "no policy yet" setup-blocker state; terms = create-new-version form + read-only history. Tests first for form validation + versioning UX.

### Task 5 — Docs + status
README settings + dev-proxy sections · E2 epic table F7 → `done` on merge · re-check `.planning/external-applications.md` (standing rule).

**Commits**: (1) migration + models + guard extension, (2) repos + service + db tests, (3) API + middleware + tests, (4) manage UI + test bootstrap, (5) docs. Review: mandatory phase-reviewer + adversarial security reviewer (RLS on four new tables, DB-enforced terms immutability + ownership guard, atomic settings merge, CSRF middleware, stored-XSS write-time validation) → both ACCEPT; one fix commit per round. PR → `gh pr checks --watch`.
