# Interview Status — standing tracker (re-flag every cycle until synthesized)

The user chose the **Full track**: the design's FINAL gate waits on real interviews. Rev-1 designs proceed critic-gated in the meantime; the F9 code build starts only after the final gate (or an explicit user directive to proceed without interviews).

| Audience | Target | Discovery done | Prototype done | Status |
|----------|--------|------|------|--------|
| Boutique owners (incl. pilot) | 2–3 | 0 | 0 | not-started — kit ready at `interview-script.md` |
| Brides (dress bought ≤18mo) | 4–5 | 0 | 0 | not-started — kit ready at `interview-script.md` |

**Each session now has two halves.** Discovery (Mom Test, `interview-script.md`) runs first; the prototype usability tasks (`../test-results.md`) run second, in the same sitting. Recruiting is the bottleneck, so we ask once and get both — and the order is fixed, because showing the design before the life questions contaminates every answer after it. Brides ~45 min total, owners ~55 min.

**Next action (user)**: schedule the pilot-boutique owner interview first; bride interviews can run in any order. Before the first session, do the font check and the dry run in `../test-results.md` → Logistics.

**Bring back per session** — the kit's five items (who · verbatim quotes · specific stories · surprises · numbers), **plus for the prototype half**: task outcomes (pass/partial/fail), where she tapped first, verbatim confusion, and time from opening the link to tapping the booking CTA.

**On results**: synthesis → severity-rated findings in `../test-results.md` → persona/journey/screen revisions → design-critic re-run → final gate → F9 build unblocks.
