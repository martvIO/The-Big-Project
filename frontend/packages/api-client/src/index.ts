// API types are generated from the backend's OpenAPI schema:
//   pnpm --filter @boutique/api-client generate   (backend running on :8000)
// The generated file lands in src/generated/schema.d.ts. The typed client
// wrapper arrives with its first consumer feature (Feature 10, booking flow).
export {};
